# SOP Compliance Checklist -- sales-agencies niche layer

Generated by Module 2D from the niche wireframe at
`research/02-niche-research/sales-agencies/09-wireframe.md` and the
components generated in Phase 6.

Load order: this per-niche layer is merged with
`website-factory/.claude/checklists/sop-compliance.universal.md` (when present)
by the sop-qa-agent at Stage 10.4b.

Gate: 95% pass rate (PASS / (PASS + FAIL), excluding N/A) on the merged checklist,
with zero em-dash hits.

---

## SECTION CHECKS

### Hero Section

- [ ] `hero.composition_matches_wireframe`, HeroSection renders the full-viewport
  dark layout defined in `09-wireframe.md` Section 1: eyebrow label, H1 headline,
  subheadline, dual CTA row (primary button + text link), trust chip row, star
  rating row, and badge row.
  Fix: Inspect `src/components/HeroSection.jsx` against the wireframe spec.

- [ ] `hero.brand_dna_paths_canonical`, HeroSection reads only
  `copy.hero.eyebrow`, `copy.hero.headline`, `copy.hero.subheadline`,
  `copy.buttonText`, `copy.heroTrustChips`, `reviews.rating`,
  `reviews.googleLabel`, `trust_badges`. No off-shape access.
  Fix: Remove any access to paths not present in `references/brand-dna.shape.js`.

- [ ] `hero.copy_from_playbook`, eyebrow label matches `niche-playbook/copy-locks.json`
  `ctaPrimary`, CTAs match `ctaPrimary` and `ctaSecondary` locked values.
  Fix: Confirm brand-dna.copy.hero and brand-dna.copy.buttonText are populated from
  the playbook at Stage 10.1.

- [ ] `hero.no_em_dashes`, zero em-dashes in rendered hero output.
  Fix: Replace em-dashes with colons, periods, or commas.

- [ ] `hero.primary_cta_anchors_form`, primary CTA button href is `#lead-form`.
  Fix: Set `href="#lead-form"` on the primary CTA button in HeroSection.jsx.

- [ ] `hero.secondary_cta_anchors_case_studies`, secondary text-link href is
  `#case-studies`.
  Fix: Set `href="#case-studies"` on the secondary CTA text link.

- [ ] `hero.geometric_grid_overlay`, SVG geometric grid overlay renders at 6% opacity
  on dark background. No stock photography in the hero.
  Fix: Restore the SVG grid overlay in HeroSection.jsx.

---

### Stats Bar Section

- [ ] `stats-bar.composition_matches_wireframe`, StatsBarSection renders a 4-cell
  dark row immediately below the hero, with count-up animation on each numeric
  value and vertical dividers between cells.
  Fix: Inspect `src/components/StatsBarSection.jsx` against wireframe Section 2.

- [ ] `stats-bar.brand_dna_paths_canonical`, StatsBarSection reads only
  `copy.heroTrustChips` and `reviews.rating`. No off-shape access.
  Fix: Remove any access to paths not present in `references/brand-dna.shape.js`.

- [ ] `stats-bar.count_up_animation`, numeric values animate via count-up using
  IntersectionObserver + requestAnimationFrame. Animation is wrapped in a
  prefers-reduced-motion guard.
  Fix: Restore useCountUp hook and guard in StatsBarSection.jsx.

- [ ] `stats-bar.no_em_dashes`, zero em-dashes in stats bar output.
  Fix: Replace em-dashes with safe punctuation.

---

### Problem Agitate Section

- [ ] `problem-agitate.composition_matches_wireframe`, ProblemAgitateSection renders
  a dark 2-column pain-point card grid with eyebrow label, H2 heading, and 2
  pain cards containing SVG icons, bold titles, and body copy.
  Fix: Inspect `src/components/ProblemAgitateSection.jsx` against wireframe Section 3.

- [ ] `problem-agitate.brand_dna_paths_canonical`, component reads only
  `copy.whyChoose.heading`, `copy.whyChoose.body`, `why_choose_us`. No off-shape access.
  Fix: Remove any access to paths not in `references/brand-dna.shape.js`.

- [ ] `problem-agitate.copy_from_playbook`, pain point framing references the
  vocabulary in `niche-playbook/vocabulary.json` (no buzzwords from blocklist).
  Fix: Cross-check default pain copy against `niche-playbook/copy-blocklist-additions.md`.

- [ ] `problem-agitate.no_em_dashes`, zero em-dashes in section output.
  Fix: Replace em-dashes with safe punctuation.

---

### Solution Section

- [ ] `solution.composition_matches_wireframe`, SolutionSection renders a light-
  background 60/40 split layout with eyebrow label, H2 heading, body paragraph,
  service cards grid, and a CTA button.
  Fix: Inspect `src/components/SolutionSection.jsx` against wireframe Section 4.

- [ ] `solution.brand_dna_paths_canonical`, component reads only
  `copy.services.label`, `copy.services.heading`, `copy.services.body`,
  `services`, `copy.buttonText`. No off-shape access.
  Fix: Remove any access to paths not in `references/brand-dna.shape.js`.

- [ ] `solution.copy_from_playbook`, CTA button text matches `copy-locks.json`
  `ctaPrimary` value.
  Fix: Verify brand-dna.copy.buttonText is sourced from playbook at Stage 10.1.

- [ ] `solution.no_em_dashes`, zero em-dashes in solution section output.
  Fix: Replace em-dashes with safe punctuation.

---

### How It Works Section

- [ ] `how-it-works.composition_matches_wireframe`, HowItWorksSection renders a
  dark 3-step horizontal row with numbered sky-blue badges, step titles, body
  copy, dashed connector lines on desktop, and a badge/subtext block at the
  bottom.
  Fix: Inspect `src/components/HowItWorksSection.jsx` against wireframe Section 5.

- [ ] `how-it-works.brand_dna_paths_canonical`, component reads only
  `copy.process.label`, `copy.process.heading`, `copy.process.body`,
  `process_steps`, `copy.process.badgeText`, `copy.process.badgeSubtext`.
  No off-shape access.
  Fix: Remove any access to paths not in `references/brand-dna.shape.js`.

- [ ] `how-it-works.step_count_three`, exactly 3 process steps render. Matches
  `niche-playbook/process.json` stepCount=3.
  Fix: DEFAULT_STEPS fallback must contain exactly 3 items.

- [ ] `how-it-works.no_em_dashes`, zero em-dashes in section output.
  Fix: Replace em-dashes with safe punctuation.

---

### Case Studies Section

- [ ] `case-studies.composition_matches_wireframe`, CaseStudiesSection renders a
  light-background grid of case study cards, each with industry tag pill, large
  outcome stat, timeframe caption, problem text, and an outlined CTA at the
  bottom of the section.
  Fix: Inspect `src/components/CaseStudiesSection.jsx` against wireframe Section 6.

- [ ] `case-studies.brand_dna_paths_canonical`, component reads only
  `copy.gallery.label`, `copy.gallery.heading`, `copy.gallery.body`,
  `previous_projects`. No off-shape access.
  Fix: Remove any access to paths not in `references/brand-dna.shape.js`.

- [ ] `case-studies.outcome_stat_dominant`, the outcome stat is the largest
  typographic element on each card (3rem+/extrabold in sky-500). Matches
  `niche-playbook/cro-rules.md` Rule 2.
  Fix: Restore text-3xl lg:text-4xl font-extrabold text-sky-500 on the stat line.

- [ ] `case-studies.minimum_two_studies`, at least 2 case study cards render
  (falls back to DEFAULT_STUDIES if fewer than 2 in brand-dna).
  Fix: Restore DEFAULT_STUDIES fallback in CaseStudiesSection.jsx.

- [ ] `case-studies.no_em_dashes`, zero em-dashes in section output.
  Fix: Replace em-dashes with safe punctuation.

---

### Objection Handling Section

- [ ] `objection-handling.composition_matches_wireframe`, ObjectionHandlingSection
  renders a dark 3-column card grid with a "What you're probably thinking" heading,
  italic objection header, and response body copy on each card.
  Fix: Inspect `src/components/ObjectionHandlingSection.jsx` against wireframe
  Section 7.

- [ ] `objection-handling.brand_dna_paths_canonical`, component reads only
  `faq` (first 3 items used as objections) and `copy.whyChoose.heading`. No off-shape
  access.
  Fix: Remove any access to paths not in `references/brand-dna.shape.js`.

- [ ] `objection-handling.three_objections`, exactly 3 objection cards render.
  Fix: Slice `faq` to first 3 items or restore DEFAULT_OBJECTIONS with 3 items.

- [ ] `objection-handling.no_em_dashes`, zero em-dashes in section output.
  Fix: Replace em-dashes with safe punctuation.

---

### Testimonials Section

- [ ] `testimonials.composition_matches_wireframe`, TestimonialsSection renders a
  light-background section with video embed placeholder, star rating display,
  aggregate review stats, and a 2-column pull-quote grid with decorative opening
  quote marks.
  Fix: Inspect `src/components/TestimonialsSection.jsx` against wireframe Section 8.

- [ ] `testimonials.brand_dna_paths_canonical`, component reads only
  `reviews.items`, `reviews.rating`, `reviews.googleLabel`, `reviews.summary`,
  `reviews.totalReviewCount`, `copy.reviews.label`, `copy.reviews.heading`.
  No off-shape access.
  Fix: Remove any access to paths not in `references/brand-dna.shape.js`.

- [ ] `testimonials.star_rating_svg`, star rating is rendered as SVG stars (not text
  glyphs), filled to reflect the numeric rating.
  Fix: Restore StarRating sub-component in TestimonialsSection.jsx.

- [ ] `testimonials.no_em_dashes`, zero em-dashes in section output.
  Fix: Replace em-dashes with safe punctuation.

---

### Risk Reversal Section

- [ ] `risk-reversal.composition_matches_wireframe`, RiskReversalSection renders as
  a full-width sky-gradient accent strip (not a dark or light section), with SVG
  grid pattern overlay, shield icon, white headline and body, and a white-background
  CTA button.
  Fix: Inspect `src/components/RiskReversalSection.jsx` against wireframe Section 9.

- [ ] `risk-reversal.brand_dna_paths_canonical`, component reads only
  `copy.offers.heading`, `copy.offers.body`, `copy.offers.detail`, `copy.buttonText`.
  No off-shape access.
  Fix: Remove any access to paths not in `references/brand-dna.shape.js`.

- [ ] `risk-reversal.explicit_guarantee`, the rendered body text includes an explicit
  guarantee statement (not "we aim to" or "we strive to" weak language).
  Matches `niche-playbook/cro-rules.md` Rule 5.
  Fix: Update `copy.offers.body` to use a risk-reversal template from
  `niche-playbook/quantified-trust-templates.md`.

- [ ] `risk-reversal.no_em_dashes`, zero em-dashes in section output.
  Fix: Replace em-dashes with safe punctuation.

---

### Pricing Anchor Section

- [ ] `pricing-anchor.composition_matches_wireframe`, PricingAnchorSection renders
  a dark centered column with eyebrow label, H2 heading, ROI anchor paragraph,
  inclusion list card, and a primary CTA button.
  Fix: Inspect `src/components/PricingAnchorSection.jsx` against wireframe Section 10.

- [ ] `pricing-anchor.brand_dna_paths_canonical`, component reads only
  `copy.offers.label`, `copy.offers.heading`, `copy.offers.detail`,
  `special_offers`, `copy.buttonText`. No off-shape access.
  Fix: Remove any access to paths not in `references/brand-dna.shape.js`.

- [ ] `pricing-anchor.no_em_dashes`, zero em-dashes in section output.
  Fix: Replace em-dashes with safe punctuation.

---

### FAQ Section

- [ ] `faq.composition_matches_wireframe`, FaqSection renders a light-background
  accordion with eyebrow label, H2 heading, and 5 to 7 FAQ items. Each item
  expands/collapses with chevron rotation on toggle.
  Fix: Inspect `src/components/FaqSection.jsx` against wireframe Section 11.

- [ ] `faq.brand_dna_paths_canonical`, component reads only `copy.faq.label`,
  `copy.faq.heading`, `faq`. No off-shape access.
  Fix: Remove any access to paths not in `references/brand-dna.shape.js`.

- [ ] `faq.accordion_accessible`, each FaqItem uses `hidden` attribute toggle
  (not CSS display none), and includes prefers-reduced-motion guard on animation.
  Fix: Restore hidden attribute logic and motion guard in FaqItem.

- [ ] `faq.item_count_range`, between 5 and 7 FAQ items render. DEFAULT_FAQS
  fallback provides 6 items.
  Fix: Verify DEFAULT_FAQS array length and slice logic.

- [ ] `faq.no_em_dashes`, zero em-dashes in FAQ output.
  Fix: Replace em-dashes with safe punctuation.

---

### CTA Section

- [ ] `cta-section.composition_matches_wireframe`, CtaSection renders a dark 50/50
  split with left copy column (formHeader, formSubtext) and right 5-field form
  (firstName, workEmail, company, revenue select, challenge textarea).
  Fix: Inspect `src/components/CtaSection.jsx` against wireframe Section 12.

- [ ] `cta-section.brand_dna_paths_canonical`, component reads only
  `copy.cta.heading`, `copy.cta.body`, `copy.formHeader`, `copy.formSubtext`,
  `copy.submitButton`, `copy.privacyLine`. No off-shape access.
  Fix: Remove any access to paths not in `references/brand-dna.shape.js`.

- [ ] `cta-section.five_form_fields`, the form contains exactly 5 fields:
  firstName, workEmail, company, revenue (select), challenge (textarea).
  Matches `niche-playbook/cro-rules.md` Rule 3.
  Fix: Restore the 5-field structure in CtaSection.jsx.

- [ ] `cta-section.form_id_lead_form`, the section element carries `id="lead-form"`
  so that all anchor CTAs resolve correctly.
  Fix: Add id="lead-form" to the section element in CtaSection.jsx.

- [ ] `cta-section.inline_confirmation`, form submission shows an inline confirmation
  message (no page redirect). Uses the locked thankYouMessage from copy-locks.json.
  Fix: Restore inline confirmation logic and CONFIRMATION_MSG constant.

- [ ] `cta-section.client_side_validation`, each required field has inline error
  message with aria-describedby linkage.
  Fix: Restore validation and aria-describedby in CtaSection.jsx.

- [ ] `cta-section.no_em_dashes`, zero em-dashes in CTA section output.
  Fix: Replace em-dashes with safe punctuation.

---

### Footer Section

- [ ] `footer.composition_matches_wireframe`, FooterSection renders a deep-dark
  (#080E14) 4-column grid with brand column (name, tagline, LinkedIn icon),
  primary nav column, secondary nav column, and contact/trust column.
  Fix: Inspect `src/components/FooterSection.jsx` against wireframe Section 13.

- [ ] `footer.brand_dna_paths_canonical`, component reads only `company.name`,
  `company.tagline`, `copy.copyright`, `copy.footerCta`, `social.linkedin`,
  `trust_badges`. No off-shape access.
  Fix: Remove any access to paths not in `references/brand-dna.shape.js`.

- [ ] `footer.semantic_markup`, footer uses semantic `<footer>` element with
  `<nav aria-label>` children for navigation groups.
  Fix: Restore semantic structure in FooterSection.jsx.

- [ ] `footer.dynamic_year`, copyright year is generated dynamically via
  `new Date().getFullYear()`, not hard-coded.
  Fix: Replace hard-coded year with dynamic call in FooterSection.jsx.

- [ ] `footer.no_em_dashes`, zero em-dashes in footer output.
  Fix: Replace em-dashes with safe punctuation.

---

## GLOBAL CHECKS

- [ ] `global.em_dash_audit`, `grep -rn` for em-dash character, `&mdash;`, and
  `&#8212;` across `src/` returns 0.
  Fix: Replace with safe punctuation (colons, periods, commas, or en-dashes
  where typographically correct).

- [ ] `global.no_placeholders`, no placeholder filenames anywhere in `public/`.
  Fix: Stage 4 / Stage 9 / manual drop required for any remaining placeholder
  assets.

- [ ] `global.cta_anchors_form`, every primary CTA either anchors to `#lead-form`
  or routes to `/contact`. No primary CTA points to an external URL or opens a
  modal that bypasses the on-page form.
  Fix: Inspect each component's CTA href against this rule.

- [ ] `global.real_logo_present`, client logo is a real SVG or PNG (not a
  placeholder filename or empty alt).
  Fix: Stage 4 asset-scraper / manual drop.

- [ ] `global.font_loaded`, Inter loads via Google Fonts per
  `brand-dna.typography.headingFontUrl` and `brand-dna.typography.bodyFontUrl`.
  Fix: Verify `index.html` preconnect link and `@import url(...)` in
  `src/index.css` after inject-theme.mjs runs.

- [ ] `global.tailwind_only`, no inline style attributes in JSX components except
  where Tailwind utility classes cannot express the value (e.g., the sky gradient
  in RiskReversalSection and the radial glow in HeroSection).
  Fix: Convert inline styles to Tailwind utilities where possible.

- [ ] `global.lighthouse_mobile_score`, Lighthouse mobile performance >= 80
  (informational; flags but does not halt by itself).
  Fix: `tools/optimise-image.py`, lazy-load, code-split.

---

## MOBILE CHECKS

- [ ] `mobile.no_horizontal_scroll`, at 375px viewport no section overflows
  horizontally.
  Fix: Per-section CSS audit; check for fixed-width elements and missing responsive
  classes.

- [ ] `mobile.touch_targets`, every interactive element (buttons, links, accordion
  toggles) is at least 44px in tap area.
  Fix: Increase padding or add a min-h-[44px] min-w-[44px] class.

- [ ] `mobile.legible_body_copy`, body copy is >= 14px effective font size at
  375px viewport.
  Fix: Adjust Tailwind config body font size or add sm:text-base class.

- [ ] `mobile.hero_single_column`, hero section collapses to single column at
  mobile breakpoint with full-width CTA buttons.
  Fix: Verify flex-col and w-full classes on mobile in HeroSection.jsx.

---

## PASS CRITERIA

The merged checklist (this per-niche layer merged with the universal layer)
must pass at 95% (PASS / (PASS + FAIL), excluding N/A) with zero em-dash hits.
Any FAIL on the following items is an immediate halt regardless of overall
pass rate:

- `global.em_dash_audit`
- `cta-section.form_id_lead_form`
- `cta-section.five_form_fields`
- `case-studies.outcome_stat_dominant`
- `risk-reversal.explicit_guarantee`

Author: Module 2D Phase 10, sales-agencies niche
Version: 1.0.0
